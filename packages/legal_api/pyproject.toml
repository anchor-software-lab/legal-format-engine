[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "legal-api"
version = "0.1.0"
description = "FastAPI SaaS surface for the Anchor Quality Gate"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "pydantic>=2.0,<3",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
    "legal-quality-gate>=0.1.0",
    "legal-format-engine>=0.1.0",
    "legal-docx>=0.1.0",
    "legal-citations>=0.1.0",
    "legal-authority>=0.1.0",
    "legal-llm-gateway>=0.1.0",
]

[project.optional-dependencies]
queue = [
    "redis>=5.0",
    "rq>=1.16",
]
crypto = [
    "cryptography>=42",
]
dev = ["pytest", "pytest-asyncio", "schemathesis"]

[tool.hatch.build.targets.wheel]
packages = ["src/legal_api"]
