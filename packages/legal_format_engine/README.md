# legal_format_engine

Rules > ML > Defaults format-spec resolver, with provenance tracking.
Originally the entire repo; now one package in the Anchor Quality Gate
workspace. Exposes `merge_with_rules`, `load_format_spec`,
`build_formatting_checker`. See the repo-root README and CLAUDE.md for
the system overview.
