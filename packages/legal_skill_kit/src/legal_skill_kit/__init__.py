"""Builds the distributable Claude skill bundle.

v1 surface (planned):
- `build.build_skill_bundle(out_dir)` — render SKILL.md + helper
  scripts + manifest into a zip-able directory.
- `templates/SKILL.md.j2` — Jinja2 template for the skill description.
"""
